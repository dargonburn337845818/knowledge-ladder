# 拆题有效性验证方案

本文件定义“动态熵减拆题”是否真的帮助用户的验证方法。代码层自动校验已经进 CI；这里主要记录**真实用户行为验证**。

## 有效性定义

一次拆题“有效”当且仅当：

1. 用户能说出一个**自己的假设**（“我觉得像……，因为……”）。
2. 用户完成一个**下一步动作**：写暴力、画图、列性质、试小样例。
3. 用户没有把界面当成“答案机器”：最终页不再依赖算法概率排名做判断。
4. 整个流程在 12 个问题内收敛，且用户知道“为什么收敛到这里”。

## 自动校验（已接入 CI）

- `scripts/validate_content.py`
  - 问题必须短、是结构性问题、不出现算法名/标签黑话。
  - 每个方向必须有 `next_actions` 和 `self_questions`。
- `scripts/engine_parity.py`
  - Python / JS 引擎输出一致。
- 单元测试 + ruff + mypy + data schema。

## 人工验证协议

### 材料

- 5 道题：每档选 1 道（例如 1400 / 1600 / 1800 / 2000 / 2200）。
- 不提前看题解、不看标签。

### 每道题记录

| 字段 | 说明 |
|---|---|
| 题号/来源 | 便于复现 |
| 使用前想法 | 一段话 |
| 使用后想法 | 一段话 |
| 是否说出假设 | 是/否 |
| 是否完成下一步动作 | 是/否 |
| 是否觉得被引导 | 1–5 |
| 是否觉得机器在猜答案 | 1–5（越低越好） |
| 是否还需要算法权重 | 是/否（预期：否） |
| 用时 | 分钟 |

### 成功标准

- 至少 4/5 道题能说出自己的假设。
- 至少 4/5 道题完成了一个下一步动作。
- “觉得机器在猜答案”平均 ≤ 2。
- “还需要算法权重”选择“否”的题 ≥ 4/5。

## 迭代规则

- 若用户普遍说“问题太抽象”，优先改 `feature_algorithm_matrix.json` 的问题，而不是改 UI 文案。
- 若用户仍觉得“机器在猜答案”，继续把算法线索后移，甚至只保留在“诊断”中。
- 若用户说“没有帮助”，则回到“先写暴力 → 列结构 → 一个小动作”的最小闭环，砍掉所有概念性内容。
