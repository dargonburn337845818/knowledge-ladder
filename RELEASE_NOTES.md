# v1.1.0 — 动态熵减拆题引导

## 新特性

- **动态问题流**：不再固定问四步，而是从 20 个特征问题里自动选择“信息增益最大”的那一个。
- **极简交互**：一次只显示一个问题，只有三个按钮：
  - 是
  - 否
  - 不确定
- **人类探测器**：随时可点“我感觉不对劲”，低熵时立即打断并弹出四大方向。
- **基线暴力优先**：必须先确认已经想了一个暴力/模拟方案，才进入动态拆题。
- **四大方向收敛**：编码压缩 / 传播松弛 / 剪枝决策 / 变换域映射。
- **候选算法权重**：
  - 问题流中实时显示 Top3 候选算法；
  - 最终页显示后验权重 ≥2% 的候选算法清单。
- **深度启发话术**：每个方向都有结合当前候选算法的个性化引导，不是机械一句话。

## 设计理念

- 机器负责大块逻辑推导：用公开题目标签做先验，用信息增益动态二分。
- 人负责异常点探测：用主观直觉强行打断机器。
- 纯本地运行，不调用云端 API，不部署大模型，只做 JSON 状态机 + log2 运算。

## 数据来源

- Codeforces 官方 API
- DMOJ 公开 API v2
- AtCoder Problems 公开数据集

## 支持平台

| 平台 | 说明 |
|---|---|
| Android | 通过 GitHub Actions 自动构建 APK |
| Windows | 通过 GitHub Actions 自动构建 EXE |

## 使用方式

1. 打开应用，先确认“基线暴力”。
2. 回答动态问题流：是 / 否 / 不确定。
3. 随时可点“我感觉不对劲”强行打断。
4. 自动收敛后查看四大方向与候选算法权重。
5. 点击方向查看详细启发。

## 可调参数

可在以下文件中手动调整：

- `algorithm_prior.json`：先验概率、熵阈值、IG 阈值、权重阈值。
- `feature_algorithm_matrix.json`：每个算法的特征概率与方向权重。
- `heuristics.json`：四大方向的启发话术。
